<%= if product.image_url.present?
  image_tag(product.image_url, class: "img-responsive product_image img-zoom", data_zoom_image: product.image_url)
else
  image_tag('noproductimage.png', class: "img-responsive product_image")
end
%>
